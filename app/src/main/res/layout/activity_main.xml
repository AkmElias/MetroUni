<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/uni1"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.rabel.after_test.MainActivity">
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
    <LinearLayout
        android:layout_width="match_parent"
        android:orientation="vertical"
        android:layout_height="wrap_content">

        <!-- Home -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/Homebtn"
        android:text="Home"
        android:layout_margin="10dp"
        android:textSize="16dp"
        android:textColor="#4CAF50"
        android:background="#364F65"
        />
        <!-- Programees -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/Homebtn"
        android:id="@+id/ProgrameesBtn"
        android:text="Programees"
        android:textColor="#4CAF50"
        android:background="#364F65"
        android:layout_margin="10dp"
        android:textSize="16dp"
        />
        <!-- Academics -->
    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/ProgrameesBtn"
        android:id="@+id/AcademicHomeBtn"
        android:text="Academics"
        android:textColor="#4CAF50"
        android:background="#364F65"
        android:layout_margin="10dp"
        android:textSize="16dp"
        />

        <!-- Current Students -->
        <Button
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/AcademicHomeBtn"
            android:id="@+id/current_studentsBtn"
            android:text="Current Students"
            android:layout_margin="10dp"
            android:textSize="16dp"
            android:textColor="#4CAF50"
            android:background="#364F65"
            />
    </LinearLayout>
    </ScrollView>
</RelativeLayout>
